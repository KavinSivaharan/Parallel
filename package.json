{
  "name": "parallel",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r build",
    "db:migrate": "docker compose exec -T postgres psql -U parallel -d parallel < apps/api/migrations/001_event_store.sql",
    "predev": "pnpm --filter './packages/**' build",
    "dev": "pnpm --parallel --filter @parallel/api --filter @parallel/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "pretypecheck": "pnpm --filter './packages/**' build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
